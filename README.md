# EcoFusion RentalCars

Estructura base del proyecto.

- `frontend/` — React + Vite
- `backend/` — Python + FastAPI
- `database/` — MySQL/XAMPP, esquema, migraciones y seeds
- `docs/` — documentación del proyecto

La conexión final será:

Frontend → FastAPI → MySQL
