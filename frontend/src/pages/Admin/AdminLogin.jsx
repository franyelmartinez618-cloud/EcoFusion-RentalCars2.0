import { useEffect, useState } from "react";
import { useAuth } from "../../context/AuthContext";
import { useApp } from "../../context/AppContext";
import { navigate } from "../../utils/router";
import "../SimplePages.css";
export default function AdminLogin(){const {translations:t}=useApp();const {user,login,loginGoogle,logout}=useAuth();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);useEffect(()=>{if(user) {if(user.role==="admin") navigate("/admin"); else {logout();setError(t.adminAuth?.clientDenied || "This account does not have administrator access.");}}},[user]);const submit=async e=>{e.preventDefault();setError("");setBusy(true);try{const u=await login(email,password);if(u.role!=="admin"){await logout();throw new Error("This account does not have administrator access.")}navigate("/admin")}catch(err){setError(err.message)}finally{setBusy(false)}};return <div className="auth-screen"><div className="auth-card"><div className="auth-brand"><span>E</span><div><strong>EcoFusion</strong><small>Administration</small></div></div><span className="page-hero__eyebrow">{t.adminAuth.eyebrow}</span><h1>{t.adminAuth.title}</h1><p>{t.adminAuth.description}</p><button type="button" className="auth-provider auth-provider--google" disabled={busy} onClick={async()=>{setBusy(true);setError("");try{await loginGoogle()}catch(err){setError(err.message)}finally{setBusy(false)}}}><span>G</span> {t.account.continueGoogle}</button><form className="simple-form" onSubmit={submit}><label>{t.account.email}<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>{t.account.password}<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label>{error&&<p className="auth-error">{error}</p>}<button type="submit" disabled={busy}>{busy?t.adminAuth.checking:t.adminAuth.signIn}</button></form><p className="auth-footer"><a href="/">← {t.common.viewSite}</a></p></div></div>}
