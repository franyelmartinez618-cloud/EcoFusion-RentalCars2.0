# Backend
Python + FastAPI. Punto único de acceso a la base de datos.


## Welcome email (optional)

Newly created accounts can receive a server-side welcome email through Resend. Configure `RESEND_API_KEY` and `RESEND_FROM_EMAIL` only in Railway secrets/environment variables. The authentication transaction does not fail when email delivery is unavailable.
